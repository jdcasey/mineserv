apiVersion: v1
kind: Service
metadata:
  labels:
    app: mineserv
  name: mineserv-lb-tcp
spec:
  type: LoadBalancer
  selector:
    app: mineserv
  ports:
  - name: main-tcp
    port: 25565
    protocol: TCP
    targetPort: 25565
